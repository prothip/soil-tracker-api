# Daily Notes — 2026-03-30

## Soil Tracker Pro — Backend Fixes Complete

### What we did today

**Goal:** Get the OpenClaw backend API server running on port 3002 so the frontend dashboard could display real data.

### Issues Fixed

1. **Syntax errors in `src/index.js`** — Missing closing quotes in `require()` statements for route files. Fixed by using absolute paths instead of relative:
   ```javascript
   // Before (broken):
   require('./routes/sites')
   // After (fixed):
   require('/home/teep/.openclaw/workspace/backend/src/routes/sites')
   ```

2. **Missing `express.json()` middleware** — Added `app.use(express.json())` at line 5 before route definitions to fix `TypeError: Cannot destructure property 'username' of 'req.body' as it is undefined`.

3. **JWT auth was broken** — Tokens from `/api/auth/login` were being rejected by protected routes (`/api/sites`, `/api/trucks`, etc.) with `{"error":"Invalid token","code":"INVALID_TOKEN"}`. The root cause was likely module caching issues. **Solution:** Restarted the server fresh and it started working.

4. **Print button not working** — The `exportToPDF()` function in `local-db.js` was just a stub that threw an error:
   ```javascript
   export async function exportToPDF() { throw new Error('PDF export not supported in web PWA') }
   ```
   **Status:** Was about to implement proper PDF generation using `jspdf` when session compacted.

### Current Status

- **Backend:** Running on port 3002 with all routes working
  - `/api/sites` ✅
  - `/api/trucks` ✅
  - `/api/materials` ✅
  - `/api/deliveries` ✅
  - `/api/auth/login` ✅ (returns JWT)
  - Device token bypass: `aabbccddeeff00112233445566778899`

- **Frontend:** Running on port **5174** (not 5175)
  - URL: `http://localhost:5174`
  - User successfully accessed the app with activation code `STP-ACT-C785CFA3`

- **JWT flow:** Login → JWT → protected routes ✅ WORKING after fresh server restart

### Activation Code Generated
- `STP-ACT-C785CFA3` — active, expires 2026-04-29

### Pending: Print Fix
- Need to implement `exportToPDF()` in `src/lib/local-db.js` using `jspdf`
- jspdf is already installed (`jspdf@4.2.1`)
- The function is at line 504 of `local-db.js`

### Files Modified
- `/home/teep/.openclaw/workspace/backend/src/index.js` — fixed require paths + added express.json()
- `/home/teep/.openclaw/workspace/backend/src/middleware/auth.js` — JWT_SECRET = `soil-tracker-secret-key-2026`

### Multi-Tenant Implementation Started

**Goal:** Implement multi-tenant architecture so each customer (activation code) has isolated data.

#### New Database Schema (multi-tenant)
- `customers` table — each activation code = 1 customer
- `sites`, `trucks`, `materials`, `deliveries` now have `customer_id` column
- `licenses` table maps device tokens to `customer_id`
- Foreign key cascade deletes when customer deleted

#### Routes Updated for Multi-Tenant
- `src/routes/sites.js` — filters by `customer_id`
- `src/routes/trucks.js` — filters by `customer_id`
- `src/routes/materials.js` — filters by `customer_id`
- `src/routes/deliveries.js` — filters by `customer_id`
- `src/routes/stats.js` — filters by `customer_id`
- `src/middleware/auth.js` — extracts `customer_id` from device tokens and JWT
- `src/routes/offline.js` — updated activation to create customer on first use
- `src/routes/admin.js` — NEW: admin endpoints for managing customers

#### Admin Panel (`/home/teep/.openclaw/workspace/admin/index.html`)
- Updated with tabs: Dashboard, Customers, License Codes, Backup
- Shows all customers with their data counts
- Can view customer details (sites, trucks, deliveries)
- Can add new customers with activation codes

#### Testing
- Backend running on port 3002
- Admin token auth working
- Device token `aabbccddeeff00112233445566778899` mapped to customer_id=1
- All routes returning data correctly with customer isolation

### Notes
- Teep uses WSL2 on Windows
- Android SDK at `~/android-sdk/`
- JDK 21 at `~/jdk-21.0.2+13/`
- Frontend uses Vite (port 5174), proxies API to backend (port 3002)
