[railway]
  name = "soil-tracker-api"
  version = 2

[deploy]
  startCommand = "cd backend && npm install && npm start"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[environment]
  NODE_ENV = "production"
